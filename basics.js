// Variablen

    // let
        // let "Variablenname" = "Variable";
        // mit let definiert man 1x eine Variable und danach kann man diese auch ändern

    let letVariable = 27;

    console.log(letVariable);

    meineVariable = 32;

    console.log(letVariable);

    // const
        // const "Variablenname" = "Variable";
        // wird 1x definiert wie let, aber lässt sich nicht mehr ändern, ist also "const"ant

    const constVariable = 12;

    console.log(constVariable);

// Datentypen

    let myString = 'http://127.0.0.1:5500/index.html';  // string sind Texte (in ' ') auch Links sind nur Texte

    let myNumber = 46.0651;   // ohne Anführungszeichen eine Zahl (geht auch Kommazahl in JS)
    let myFloat = 32.0;       // Float ist eine Kommazahl
    let myInt = 987;          // Integer ist eine Ganzzahl

    let myBoolean = true;       // Boolean ist nur true oder false

    let myArray = [2,5,'asdf'];     // Array ist eine Liste in []

    let myObject = {age : 75, height : 178};    // ein Object ist eine Liste mit key/value Paar

